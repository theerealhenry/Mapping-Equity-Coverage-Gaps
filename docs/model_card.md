# Model Card

Populated at Stage 11 (Documentation), reporting on the explanatory model built in Stage 8 —
including the honest go/no-go outcome (if the winning model's explanatory power is weak, that
outcome is documented here directly, e.g. "the explanatory model was evaluated and found
insufficiently explanatory, so it was not deployed," rather than forcing a weak model into the
writeup or the app for its own sake).

Empty as of Stage 4 — no model exists yet to document.
