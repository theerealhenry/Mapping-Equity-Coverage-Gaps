# Risk Register

A dynamic risk register, per `PROJECT_BLUEPRINT.md`'s documentation section: each entry carries an
ID, description, probability, impact, a probability x impact score, mitigation, status, an owner,
and a last-reviewed date — not a static table filled once. Seeded at Stage 4 with the six risks
already identified across Stages 1-3's own real findings and audits (transcription of already-done
work into its intended final home, not speculative future content); new risks are added as they're
identified in later stages.

| ID | Description | Probability | Impact | Score | Mitigation | Status | Owner | Last reviewed |
|---|---|---|---|---|---|---|---|---|
| R-001 | GEOID-as-integer truncation — an integer-typed GEOID silently drops a leading-zero state FIPS (e.g. Maricopa's `04...` becomes `4...`), corrupting every downstream tract join. | High | Critical | High | `src/io.py`'s `_assert_geoid_is_string` guard, applied to every strata/national-strata/sample-submission load; `src/schemas.py`'s pandera contract for CBP's GEOID. Hardened further in the Stage 3 review pass to correctly handle a null-containing but otherwise valid string GEOID column (`docs/data_manifest.md` Section 4.19). | Mitigated | Henry Otsyula | 2026-09-06 |
| R-002 | Incorrect CRS axis order — DuckDB's `spatial` extension follows EPSG authority axis order for the literal string `EPSG:4326` (lat/lon), the opposite of `OGC:CRS84` (lon/lat), silently producing `POINT(inf inf)`/NaN results if used carelessly. | High | Critical | High | Centralized CRS utility in `src/geometry.py` (`GEOGRAPHIC_CRS`, `assert_crs84`, `normalize_geographic_crs`) — every transform goes through this one shared, tested module, never ad hoc per script; the literal string `"EPSG:4326"` is never written anywhere else in the codebase. | Mitigated | Henry Otsyula | 2026-09-06 |
| R-003 | ACS housing units used in a denominator — a plausible-looking but incorrect choice for a coverage-gap ratio's denominator, given the challenge's actual reference layers. | Medium | High | Medium | To be shown visually, not just asserted in prose, in `notebooks/03_building_vs_housing_analysis.ipynb` (Stage 7) — why ACS housing-unit counts are excluded from any denominator in this project's formula. | Open — planned for Stage 7 | Henry Otsyula | 2026-09-06 |
| R-004 | Hospital inclusion in the facilities count — Overture over-counts hospitals roughly 12x relative to HIFLD, which would make the facilities term never show a real deficit if hospitals were included. | High | High | High | Hospitals deliberately excluded from the facilities half of the POI gap, confirmed directly from live Overture `categories.primary` sample data (`docs/data_manifest.md` Section 1 / verification findings) — a data-driven exclusion, not an assumption. | Mitigated (decision made; implementation lands with `src/gaps.py` in Stage 7) | Henry Otsyula | 2026-09-06 |
| R-005 | Zeroing vs. excluding undefined coverage-gap components — silently treating an undefined component (e.g. `reference=0`) as a zero gap would understate real coverage gaps and bias the composite score. | Medium | Critical | High | Formula design excludes undefined components from the mean entirely, never zeroes them — locked down by `tests/test_gap_arithmetic.py`'s golden-case fixtures before the pipeline ever runs against real data (Stage 7). This exact rule is also the structural basis for the Measurement Eligibility Bias flagship Bias Discovery candidate (Stage 9). | Open — planned for Stage 7 | Henry Otsyula | 2026-09-06 |
| R-006 | Unfiltered road classes — a naive road-network count without the documented class filter would include minor/service roads never intended to count toward the transport gap, and Overture/TIGER near-miss category values (the same risk class already proven real for POI categories) could silently miscount. | Medium | High | Medium | Exact-match filtering: `MTFCC in (S1100, S1200)` for TIGER, `class in (motorway, trunk, primary, secondary)` for Overture — real-data verification of both against the documented filter is Stage 5's first EDA self-check, before any building/POI logic is needed. | Open — planned for Stage 5 | Henry Otsyula | 2026-09-06 |
